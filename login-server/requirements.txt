# login-server/requirements.txt
# Python >= 3.12 required (ctypes SSL pointer layout assumed for 3.12;
# if upgrading to 3.13+, replace the ctypes workaround in app.py with the
# native ssl.SSLSocket.export_keying_material() added in 3.13).

# ── Web framework ────────────────────────────────────────────────────────────
Flask==3.1.3
Werkzeug==3.1.7

# ── ORM / database ───────────────────────────────────────────────────────────
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.51

# ── Password / key derivation ─────────────────────────────────────────────────
# PW_KEY is derived via hashlib.pbkdf2_hmac("sha256", ...) in app.py's
# _generate_pw_key() — stdlib, no external dependency required.
# (Previously used argon2-cffi for Argon2id; removed when app.py switched
# to PBKDF2-SHA256.)
